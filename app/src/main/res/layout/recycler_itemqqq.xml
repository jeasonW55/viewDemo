<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <RelativeLayout
        android:layout_alignParentTop="true"
        android:visibility="visible"
        android:id="@+id/item_top_layout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_50">

    </RelativeLayout>
    <View
        android:id="@+id/vertical_middle_divider"
        android:layout_below="@id/item_top_layout"
        android:background="#FF0000"
        android:layout_width="match_parent"
        android:layout_height="2dp"/>

    <RelativeLayout
        android:layout_below="@id/vertical_middle_divider"
        android:background="@color/black"
        android:visibility="gone"
        android:id="@+id/item_bottom_layout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_50">

    </RelativeLayout>

    <View
        android:layout_below="@id/item_bottom_layout"
        android:id="@+id/bottom_divider"
        android:background="@color/green"
        android:layout_width="match_parent"
        android:layout_height="1dp"/>
</RelativeLayout>